#include "default.h"


int get_gross_pay(int hours = 30, int pay_rate = 20);//get_gross_pay(40);
{
return hours * pay_rate;

}
int get_gross_pay(int hours, int pay_rate, double sales)

    auto gross_pay = 0;
    
    gross_pay = hours * pay_rate;
    gross_pay += sales * .o1;

    return gross_pay;